#include "main.h"

/**
 * _strlen_recursion - function that return
 * @s: param
 */

int _strlen_recursion(char *s)
{

	int i;
	int length;

	i = 0;
	length = 0;

	if (s[i] != '\0')
		length++;
		i++;
	return _strlen_recursion;
}
