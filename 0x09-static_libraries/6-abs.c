#include "main.h"

/**
 * _abs - function return tha absolute value
 * @n: parameter for the number given.
 * Return: Always n.
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
