#include <stdio.h>

/**
 * main - Entry point
 * @argc: count of arguments
 * @argv: string of arguments
 * Return: Always 0.
 */

int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
