#include <stdio.h>

/**
 * main - Entry point
 * @argc: count arguments
 * @argv: string of arguments
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
