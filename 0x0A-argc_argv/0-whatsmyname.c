#include <stdio.h>

/**
 * main - Entry point
 * @argc: param
 * @argv: param
 * Return: Always 0.
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
