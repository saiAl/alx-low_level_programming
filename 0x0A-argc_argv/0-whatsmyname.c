#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 * @argc: count arguments
 * @argv: string of arguments
 * Return: Alawys 0.
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
